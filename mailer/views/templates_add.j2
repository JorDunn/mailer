{% extends "base.j2" %}

{% block content %}
<br/><br/>
<div class="col-lg-8 offset-lg-2">
    {% with messages = get_flashed_messages(category_filter=['template_error']) %}
        {% for message in messages %}
        <div class="alert alert-danger" role="alert">
            {{ message }}
        </div>
        {% endfor %}
    {% endwith %}
    <div class="card">
        <div class="card-body">
            <h5 class="card-title text-center">Add Template</h5>
            <form action="/templates/add/do?token={{ token }}" method="post">
                <div class="form-group">
                    <label for="title">Title</label>
                    <input type="text" name="title" class="form-control">
                </div>
                <div class="form-group">
                    <label for="base">Template Base</label>
                    <input type="text" name="base" class="form-control">
                </div>
                <div class="form-group">
                    <label for="template">Template Code</label>
                    <textarea name="template" class="form-control"></textarea>
                </div>
                <div class="form-group">
                    <label for="expires">Expires on </label>
                    <input type="date" name="expires" class="form-control">
                </div>
                <button class="btn btn-primary float-right" type="submit"><span class="oi oi-plus"></span>&nbsp;&nbsp;Add</button>
            </form>
        </div>
    </div>
</div>
{% endblock %}